#!/bin/sh
#
# clear out builds and downloaded sources from ~/.cpan
#

rm -rf $HOME/.cpan/sources/authors $HOME/.cpan/build
rm -rf $HOME/.cpanplus/authors $HOME/.cpanplus/*/build $HOME/.cpanplus/install-logs
