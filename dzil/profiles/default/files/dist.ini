{{
    $username = 'mschout';

    $license = ref $dist->license;
    if ( $license =~ /^Software::License::(.+)$/ ) {
        $license = $1;
    } else {
        $license = "=$license";
    }

    $authors .= $_ for @{$dist->authors};
    '';
}}name = {{$dist->name}}
author = {{$authors}}
license = {{$license}}

[@MSCHOUT]
