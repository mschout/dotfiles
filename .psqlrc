-- require commit/rollback to make changes
\set AUTOCOMMIT off

-- show nulls as NULL
\pset null 'NULL'

-- history file for each db
\set HISTFILE ~/.psql_history- :DBNAME

\set HISTSIZE 5000

\echo 🧐 🐘 postgres_dba 6.0 installed. Use ":dba" to see menu
\set dba '\\i /home/mschout/Sync/git/postgres_dba/start.psql'
